import {PacmanLoader} from "react-spinners";

const Spinner = () => {
  return(
    <div className={"Spinner"}>
      <PacmanLoader size={20} color={'#5b42f3'}/>
    </div>
  )
}

export default Spinner